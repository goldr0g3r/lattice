{
  "name": "@lattice/core-bindings",
  "version": "0.1.0",
  "private": true,
  "description": "Auto-generated TypeScript bindings for the Rust core (lattice-core). Emitted by ts-rs from the build script of core/lattice-core.",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "build": "tsc --build"
  },
  "devDependencies": {
    "@lattice/tsconfig-preset": "workspace:*",
    "typescript": "~6.0.3",
    "vitest": "^2.1.8"
  }
}
